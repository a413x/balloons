{
  "name": "balloons",
  "version": "1.0.0",
  "scripts": {
    "start": "serve ./public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^11.3.2"
  }
}
